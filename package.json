{
  "name": "specterjs",
  "version": "0.1.0",
  "private": true,
  "description": "Modern browser integrity & anti-fingerprinting lie detection framework",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
